{
  "_comment": "GitHub Ruleset for the default branch. Apply via: gh api repos/:owner/:repo/rulesets --method POST --input .github/rulesets/main-protection.json. Replaces classic branch protection.",

  "name": "main-protection",
  "target": "branch",
  "enforcement": "active",
  "conditions": {
    "ref_name": {
      "include": ["refs/heads/main", "refs/heads/master"],
      "exclude": []
    }
  },
  "rules": [
    { "type": "deletion" },
    { "type": "non_fast_forward" },
    {
      "type": "pull_request",
      "parameters": {
        "required_approving_review_count": 1,
        "dismiss_stale_reviews_on_push": true,
        "require_code_owner_review": true,
        "require_last_push_approval": true,
        "required_review_thread_resolution": true
      }
    },
    {
      "type": "required_status_checks",
      "parameters": {
        "strict_required_status_checks_policy": true,
        "required_status_checks": [
          { "context": "lint-test" },
          { "context": "review" },
          { "context": "security-review" },
          { "context": "harness-validate" },
          { "context": "evidence-gate" }
        ]
      }
    },
    {
      "type": "required_signatures"
    },
    {
      "type": "required_linear_history"
    }
  ],
  "bypass_actors": []
}
