---
name: 0000-post-mortem-template
description: Post-mortem for an abandoned initiative — distinct from incidents/ (production failures) and decisions/ (forward-looking ADRs). Round 7 D.
metadata:
  type: post-mortem
  status: template
# ─── identity ──────────────────────────────────────────────────────────
initiative: <id>
slug: <kebab-case>
status: closed
abandoned_at: YYYY-MM-DD
duration_days: <number>
sunk_cost_usd: <number>     # populated by cost-report.sh --by-initiative
written_by: extractor       # or human
written_at: YYYY-MM-DDTHH:MM:SS
pivot_id: PIV-NNN           # link to the pivot manifest
---

# Post-mortem: <Initiative Title>

> Distinct from a production incident postmortem. This is for **a bet that didn't pay off** — when we abandon an initiative, drop a feature mid-flight, or supersede a roadmap commitment.

## Hypothesis

What did we believe was true when we started? State it crisply.

- "We believed X would drive Y, measured by Z."
- "We assumed customer segment W needed feature V."

## Bet

What did we build? What did we commit?

- Phases shipped: <count>
- Specs in flight when abandoned: <count>
- Code written: <LOC estimate>
- Team-weeks invested: <number>

## Disconfirming evidence

What we learned that invalidated the hypothesis. **Cite sources** — dashboards, customer quotes, metrics, ADR-XXX.

- <Evidence #1 with URL/path>
- <Evidence #2>
- <Evidence #3>

## Root cause of the bet failing

Pick one (or more):

- **Technical**: chosen architecture couldn't scale / integrate / deliver acceptably
- **Market**: target customer segment didn't materialize / shrank / pivoted
- **Timing**: right idea, wrong time (e.g., dependency on external X not yet mature)
- **Capacity**: team couldn't deliver in the appetite window
- **Strategic**: leadership shifted priorities; this was deprioritized externally
- **Customer feedback**: actual usage contradicted assumed need

Then expand in 2-3 sentences.

## What we'd do differently

If we re-encountered this hypothesis tomorrow:

- ...
- ...

## What survives

The valuable output of an abandoned initiative. Populated by the extractor agent:

- **ADRs still applicable** (marked `orphaned_from: <initiative>`):
  - [path/to/adr.md]
  - [path/to/another.md]
- **Patterns discovered**:
  - [`.claude/memory/patterns/<slug>.md`]
- **Code reusable**:
  - [path/to/file.ts] — refactor target: `shared/utils/X`
  - [path/to/lib.py] — useful as-is, just relocate
- **Test fixtures**:
  - [path/to/fixtures]
- **Customer signals captured during the work**:
  - [`.claude/memory/feedback/FB-...`] — keep for next attempt

## What does NOT survive

The disposable / harmful output:

- **Anti-patterns** (now documented in `.claude/memory/anti-patterns/`):
  - [`anti-patterns/<slug>.md`] — "we tried X, it produces Y problem"
- **Code to delete** (don't extract — just remove):
  - [path/to/feature/specific.ts]
- **Dependencies to remove** (added solely for this initiative):
  - [package@version]

## Reusable code — salvage tasks

Auto-generated by the extractor as TASKS.md entries with `priority: cleanup`:

- T-NNN: Extract `<file>:<lines>` to `shared/utils/<name>`
- T-NNN: ...

## References

- Pivot manifest: `pivots/active/PIV-NNN-*.md` (or `pivots/archive/` if closed)
- Initiative: `initiatives/archive/<id>-*.md`
- Sunk-cost report: `.claude/memory/audits/sunk-cost-<initiative>-<date>.md`
- Decision log: `.swarms/coordinator/decisions.log` (search for the initiative id)
- Customer feedback that informed (or invalidated) the hypothesis: `<FB-IDs>`
- ADRs in scope: <grep result>

## Re-encounter discipline

If a future spec/initiative proposes a similar hypothesis:
- The architect agent's "READ MEMORY FIRST" step (Round 4 F) should surface this post-mortem
- The pattern of "we tried X" must be explicitly addressed in the new spec's `## Why this time is different` section
- If the new attempt also fails, prepend an entry: `recurred_at: YYYY-MM-DD`
