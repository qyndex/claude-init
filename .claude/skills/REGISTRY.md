# Skill Registry

_Auto-generated by .claude/scripts/regen-skill-registry.sh — do not hand-edit._
_Last generated: 2026-06-12T13:51:34+10:00_

| Skill | Description | Auto-invoke | Model |
|---|---|---|---|
| `analyze` | Cross-check spec, plan, and tasks for consistency. Phase 4.5 of the eight-phase workflow. Catches scope drift, missing a | auto | sonnet |
| `animation-discipline` | Use when adding transitions, hover states, scroll triggers, motion. Enforces easing/duration discipline, prefers-reduced | auto | sonnet |
| `api-versioning` | Manage public API version transitions — v2-alongside-v1, Deprecation/Sunset headers, client migration tracker. Use whe | auto | opus |
| `autopilot` | Five-phase autonomous execution. Spec expansion → planning → execution → QA loops (max 5) → multi-perspective va | auto | sonnet |
| `browser-e2e` | Browser-driven E2E discipline. Thin pointer — defers to anthropics:webapp-testing plugin for Python Playwright scripts | auto | inherit |
| `characterize` | Write characterization (approval/golden-master) tests that pin the CURRENT behavior of untested legacy code BEFORE it is | auto | opus |
| `clarify` | Walk through every open question in a spec and resolve it with the user. Phase 2.5 of the eight-phase workflow. Modeled  | auto | sonnet |
| `codemod` | Large-scale refactor via AST-aware transforms — jscodeshift (JS/TS), ts-morph (TS), comby (any language), bowler (Pyth | auto | sonnet |
| `constitution-compact` | Quarterly constitution compaction. Identifies redundant, stale, or over-verbose sections in .claude/CLAUDE.md, proposes  | auto | sonnet |
| `constitution` | Author or amend the project constitution at .claude/CLAUDE.md. Use when starting a brand-new project, when a major archi | auto | opus |
| `context-budget` | Inventory and recommend prunes for loaded agents, skills, MCP servers, and rules. Reports always-needed / sometimes-need | auto | sonnet |
| `creative-web` | Use when a spec EXPLICITLY warrants wow factor beyond default Tailwind — parallax, 3D, generative art, scroll-triggere | auto | opus |
| `db-migration` | Expand/contract pattern for zero-downtime database schema changes. Replaces the (wrong) \forward-only by default\ rule.  | auto | opus |
| `debug` | Run the four-phase systematic debugging loop (reproduce → isolate → diagnose → fix). Delegates to debugger agent.  | auto | opus |
| `detect-stack` | Detect the project's tech stack(s) — frameworks, ORMs, build tools, test runners — from lockfiles and config files.  | no (operator-only) | inherit |
| `dispatch-criteria` | Six gates for deciding whether to delegate to a subagent or do it in-session. From ChristopherKahler/paul. The is this w | auto | inherit |
| `dream` | Memory consolidation. Dedupe and compress .claude/memory/ topic files, absolutize relative dates, archive stale instinct | auto | sonnet |
| `experiment` | A/B test design — hypothesis, MDE, sample size, primary/guardrail metrics, significance gate. Different from flag-roll | no (operator-only) | opus |
| `fastapi` | Use when project is FastAPI (Python async web framework). Covers Pydantic v2 models, dependency injection via Depends, a | auto | sonnet |
| `flag-cleanup` | Remove a feature flag from code after it's been 100% for the cleanup_after window. Greps call sites, replaces `if (flags | no (operator-only) | sonnet |
| `flag-rollout` | Phased rollout discipline for production. Ship code default-OFF, ramp 1%→10%→50%→100% with monitoring windows, aut | auto | opus |
| `flask-realtime` | Conventions for a Flask REST API with WebSockets (Flask-SocketIO). Invoke when atlas shows `flask` in pyproject/requirem | auto | inherit |
| `framework-detector` | Maps a detected stack (from `detect-stack` / atlas) to the framework-specific SKILL.md that should be activated for this | no (operator-only) | inherit |
| `front-end-design` | Use when designing a new UI, redesigning a screen, building a landing/marketing page, or making something look better. O | auto | opus |
| `grill-me` | Use when authoring an initiative, clarifying interdependent open questions, or designing a multi-quarter roadmap — dep | auto | sonnet |
| `handoff` | Structured handoff between subagents, between background sessions, or between the night-run and the morning session. Use | auto | inherit |
| `image-generation` | Use when a user-facing visual asset is needed (hero, illustration, icon, OG card, badge, mock). Orchestrates reuse-first | auto | sonnet |
| `implement` | Execute tasks from tasks/TASKS.md one at a time using strict TDD. Phase 5 of the eight-phase workflow. Delegate each tas | auto | opus |
| `initiative` | Author or amend a multi-quarter initiative — the program-level artifact above per-feature specs. Use for 3+ month effo | no (operator-only) | opus |
| `instinct` | Continuous-learning observer. PostToolUse + Stop hooks write JSONL observations; a background Haiku subagent extracts at | auto | inherit |
| `loop` | Run a continuous Ralph-style autonomous loop until a budget or success condition is hit. Each iteration restarts with a  | auto | sonnet |
| `nextjs` | Use when project is Next.js (App Router or Pages Router). Covers Server vs Client Components, Server Actions, Suspense b | auto | sonnet |
| `observability` | Scaffold structured logs, metrics, and trace spans for any new endpoint or service. Verify emissions in PR diffs. Enforc | auto | sonnet |
| `okr-align` | Check that a spec or initiative aligns with a current-quarter OKR. Run automatically during /specify (Phase 2) and /init | no (operator-only) | inherit |
| `parallel-swarm` | Coordinator-led parallel feature development. 5-10 features built simultaneously via native `claude --bg --worktree feat | no (operator-only) | sonnet |
| `plan` | Author the technical plan for a spec. Phase 3 of the eight-phase workflow. Produces plans/active/<id>-<slug>.md with arc | auto | sonnet |
| `prime-discipline` | Reference card of discipline patterns from across the research — Boris Cherny's CLAUDE.md / compaction / rewind rules, | auto | inherit |
| `prod-observability` | Scaffold the PRODUCTION observability + alerting + notification stack into a shipped app — OTEL collector, Sloth SLO b | auto | sonnet |
| `react-node` | Conventions for a React frontend talking to a Node.js backend (Express / Fastify / Hono). Invoke when the atlas shows bo | auto | inherit |
| `react-vite` | Conventions, pitfalls, and canonical patterns for a React + Vite SPA. Invoke when editing files under a project whose at | auto | inherit |
| `release-train` | Cadenced release process — weekly cut on Friday, staging Monday, canary Wednesday, 100% Friday next. Different from re | no (operator-only) | sonnet |
| `research` | Ground a decision in evidence. Delegates to researcher agent. Produces a citation-heavy comparison brief saved under doc | auto | sonnet |
| `review` | Run code review and (optionally) security review on the current diff. Phase 7 of the eight-phase workflow. Delegates to  | auto | opus |
| `security-guard` | Project security discipline. Thin pointer that wires up the semgrep MCP, CodeQL workflow, dependency audits, and the OWA | auto | inherit |
| `self-heal` | When a test fails, lint errors, or CI breaks — diagnose with debugger agent, propose minimal fix, implementer applies, | auto | inherit |
| `ship` | Final phase — push branch, open PR, wait for CI, merge, deploy. Phase 8 of the eight-phase workflow. Delegates to rele | auto | sonnet |
| `skill-creator` | Use when a recurring task pattern emerges that deserves its own skill. Generates a fresh SKILL.md under .claude/memory.p | auto | sonnet |
| `specify` | Author a feature spec under specs/active/. Captures problem, user stories, acceptance criteria, constraints, open questi | auto | opus |
| `tailwind-discipline` | Use when writing Tailwind utility classes. Enforces utility-first discipline, semantic token usage, no inline style, pre | auto | sonnet |
| `tasks` | Decompose an approved plan into 2-5 minute atomic tasks with machine-verifiable acceptance commands. Phase 4 of the eigh | auto | sonnet |
| `tdd-loop` | The strict red→green→refactor loop, mechanically enforced. Write a failing test FIRST (red), capture the failure as  | auto | inherit |
| `token-budget` | Per-session token discipline — cache, delegate, compact. Use when output is getting verbose, context is filling, or ca | auto | sonnet |
| `typography` | Use when setting fonts, type scale, headings, body text, or measure. Enforces typographic hierarchy, readable measure, s | auto | sonnet |
| `verify-loop` | The /loop wrapper that runs autopilot until 100% verified or budget exhausted. Combines /loop, autopilot skill, verifica | auto | opus |
| `verify` | Run the actual application end-to-end and exercise the user journey from the spec. Phase 6 of the eight-phase workflow.  | auto | sonnet |
| `wip-checkpoint` | Continuous WIP-commit discipline for long features. Auto-commit at every decision point so the work survives crashes, te | auto | inherit |

## Stack mapping (for stack-aware suggestions)

Atlas-detected stack → recommended skills:

- Vite + React → react-vite, front-end-design, tailwind-discipline
- Next.js (any router) → nextjs, front-end-design, tailwind-discipline
- React + Express/Fastify/Hono → react-node
- React + tRPC → react-node
- FastAPI → fastapi
- Flask + SocketIO → flask-realtime
- Any UI stack → typography, animation-discipline (load on need)
